# PLAN.md — NEXA AI

> Plataforma inteligente para estudiar, crear y trabajar.
> Este documento es la fuente de verdad del progreso del proyecto. Se actualiza al cerrar cada fase.

## 0. Estado del proyecto

- Auditoría inicial: directorio vacío, sin código previo que reutilizar.
- Repositorio git: inicializado localmente (commit inicial `cf183a2`). Sin remoto de GitHub aún — se añadirá cuando el usuario provea uno o pida crearlo.
- Fase 1 completada: lint, typecheck y build pasan sin errores.
- Proyecto Supabase: el usuario creó manualmente su propio proyecto en otra cuenta (id `khpqeqiimsqqwhyphuon`). El proyecto `nexa-ai` creado inicialmente por el asistente (`tgdkomcqnoarcdhayztr`) queda sin usar; el usuario decide qué hacer con él.
- Migraciones (`profiles` + RLS + triggers) aplicadas manualmente por el usuario vía SQL Editor, usando los archivos en `sql-para-ejecutar/`. Verificado con una consulta REST directa (200 OK, tabla existe y RLS activo).
- Fase 2 completada en código con **email + contraseña** en vez de Google OAuth (ver decisión arriba): clientes Supabase (browser/server), `proxy.ts` con protección de rutas, signup/login/logout, confirmación de correo (`app/auth/confirm`), tabla `profiles` con RLS y trigger de auto-creación. Verificado: build OK, protección de rutas redirige correctamente a `/login`.
- Ajuste posterior: editar la plantilla de email requiere SMTP propio en Supabase Free (no disponible), así que se usa el `{{ .ConfirmationURL }}` por defecto junto con `emailRedirectTo` apuntando a `app/auth/callback/route.ts` (`exchangeCodeForSession`). Requirió agregar `http://localhost:3000/auth/callback` a Authentication → URL Configuration → Redirect URLs.
- **Fase 2 verificada end-to-end por el usuario en el navegador real**: registro con correo real → email de confirmación recibido → clic en el enlace → sesión creada → dashboard mostrando el saludo personalizado. Fase 2 cerrada.
- **Fase 3 completada**: onboarding (selección Estudiante/Profesional, guarda `role` en `profiles`), layout `(app)` con sidebar (desktop) y menú lateral (móvil, `Sheet`), dashboard real con saludo, herramientas recomendadas por perfil (marcadas "Próximamente" — el catálogo es real pero las herramientas aún no están implementadas), y placeholders honestos en `/documents` y `/history`. Páginas `/profile` (editar nombre) y `/settings` (cambiar rol, tema claro/oscuro/sistema con `next-themes`) completamente funcionales. Verificado por el usuario en el navegador: login → redirección automática a onboarding (cuenta sin rol) → selección de perfil → dashboard con sidebar. Lint, typecheck y build sin errores.
- **Fase 4 completada**: tabla `documents` (RLS por `user_id`) y bucket privado `documents` en Storage (política RLS por carpeta = `user_id`), aplicados manualmente por el usuario vía `sql-para-ejecutar/03` y `/04`, verificados directamente contra la API REST/Storage. Subida con validación real (extensión + tamaño + firma de bytes, no solo MIME declarado por el cliente), límite diario (`MAX_DOCUMENTS_PER_DAY`), listado, descarga con URL firmada, eliminación. Dashboard actualizado para mostrar documentos reales recientes. **Verificado end-to-end por el usuario en el navegador**: subida, aparición en la lista, descarga y eliminación funcionan correctamente.
- **Fase 5 completada**: capa de abstracción de IA (`lib/ai/provider.ts`, `gemini.ts`, `groq.ts`, `service.ts`). Antes de escribir el código se verificó por búsqueda web que `gemini-2.5-flash` se retira el 2026-10-16, por lo que se usó el modelo GA actual `gemini-3.8-flash` (y `llama-3.3-70b-versatile` para Groq), ambos overrideables por env var. `generateText()`/`generateStructured()` verificados end-to-end contra la API real de Gemini con la key del usuario (texto plano y modo JSON estructurado), mediante una ruta de diagnóstico temporal creada, probada y eliminada antes de commitear. Ningún tool de negocio consume la capa todavía — eso empieza en la Fase 6. Ver `docs/AI.md`.
- **Fase 6 completada**: Corrector APA 7, primer tool real con arquitectura híbrida. Reglas programadas (`lib/rules/apa.ts`) verifican márgenes, tipografía, tamaño, interlineado, sangría, numeración de página y presencia de sección de referencias, leyendo directamente el XML del DOCX (`lib/documents/extract/docx.ts`, vía `jszip` + `fast-xml-parser`) — el formato de extracción se verificó generando un DOCX de prueba con la librería `docx` e inspeccionando su XML real antes de escribir el parser, en vez de asumir la estructura OOXML de memoria. La IA (`lib/ai/prompts/apa.ts`) evalúa solo redacción/coherencia/citas contextuales, nunca formato. Tablas `ai_sessions` y `document_analyses` creadas (con `user_id` añadido a `document_analyses` para RLS simple). Límite diario de IA (`MAX_AI_REQUESTS_PER_DAY`) implementado desde este primer consumidor. UI en `/tools/apa`: hallazgos de reglas por párrafo se agregan en un solo item con botón "Ver párrafos" en vez de listar uno por párrafo (ajuste pedido por el usuario tras ver la lista demasiado extensa). Verificado end-to-end por el usuario con documentos reales, incluyendo un 503 transitorio real de Gemini manejado correctamente por el fallback de `AIService`. Ver `docs/AI.md` y `docs/DATABASE.md`.
- **Fase 7 completada**: generación de DOCX corregido (`lib/documents/generate/docx-corrections.ts`). Aplica márgenes (1"), interlineado doble, sangría de primera línea (0.5"), fuente y tamaño directamente sobre el XML OOXML (`word/document.xml` + `word/styles.xml`), respetando el orden de propiedades exigido por el schema (`w:pPr`/`w:rPr`) mediante inserción ordenada (`setOrderedChild`) en vez de solo anexar claves — evita generar un DOCX que Word marque como corrupto. No reescribe contenido, citas, numeración de página ni referencias (requieren revisión humana). `generateCorrectedDocument()` en `lib/tools/apa/actions.ts` sube el resultado a Storage (`{user_id}/corregidos/...`), actualiza `documents.processed_storage_path`/`status`, y `revalidatePath("/documents")` para que la fila del documento muestre de inmediato el botón de descarga de la versión corregida (`components/documents/document-row.tsx`). Puntajes de formato y redacción separados (`formatScore`/`writingScore` en vez de un score único) por ser evaluaciones de naturaleza distinta. `fetchWithRetry()` añadido a `lib/ai/provider.ts` (reintentos con backoff en 429/503) y usado por ambos providers. UI de `/tools/apa` optimizada: barra de progreso con color según puntaje, animaciones de entrada, ícono de carga en el botón de análisis. **Verificado de extremo a extremo con un DOCX real y la API real de Gemini** (script temporal, eliminado tras la prueba): análisis correcto, XML del documento corregido inspeccionado confirmando márgenes/interlineado/fuente/sangría aplicados, y el archivo resultante se abre y extrae texto sin corrupción. Lint, typecheck y build sin errores.
- **Landing page optimizada**: los botones "Comenzar gratis" apuntaban todos a `/login` en vez de `/signup` — un visitante nuevo caía en el formulario de inicio de sesión sin ruta obvia a registro. Corregido (header, hero y CTA final apuntan a `/signup`; se añadió un enlace "Iniciar sesión" separado en el header para usuarios existentes). Se agregaron animaciones de entrada y hover en tarjetas/botones, consistentes con el resto de la app.
- **Fase 8 completada**: Analizador de escritura (`/tools/analizador-escritura`), segundo tool con arquitectura híbrida. **Decisión ética importante**: esto NO es un detector de texto generado por IA — ningún método basado en señales superficiales de estilo puede determinar con certeza el origen de un texto, y presentarlo como tal sería engañoso y potencialmente dañino (acusaciones falsas a estudiantes). Se implementó como indicadores de estilo a revisar: reglas deterministas (`lib/rules/writing.ts`) miden variedad de longitud de oraciones (coeficiente de variación), diversidad léxica (type-token ratio), oraciones repetidas y muletillas académicas genéricas; el prompt de IA (`lib/ai/prompts/writing.ts`) tiene una regla estricta explícita de no afirmar ni insinuar autoría por IA ni dar "porcentaje de probabilidad", limitándose a describir patrones de estilo observables. La UI (`WritingForm`) incluye un disclaimer explícito de esto antes de mostrar resultados. `RuleFinding`/`RuleSeverity` se extrajeron de `lib/rules/apa.ts` a `lib/rules/types.ts` (con re-export para no romper imports existentes) para compartirlos entre ambos tools sin acoplar uno al otro. Verificado end-to-end con un DOCX de prueba deliberadamente repetitivo/uniforme contra la API real de Gemini: las reglas detectaron correctamente la uniformidad y las muletillas, y la respuesta de la IA describió el patrón sin afirmar autoría por IA en ningún momento (cumplimiento verificado leyendo la respuesta real, no asumido). También se confirmó en vivo el límite real de cuota gratuita de Gemini (20 solicitudes/día), que coincide con `MAX_AI_REQUESTS_PER_DAY` — el fallback/manejo de error de `AIService` respondió correctamente. Lint, typecheck y build sin errores.
- **Fase 9 completada**: Chat con documentos (`/tools/chat-documento`). Nueva tabla `document_chat_messages` (migración `20260921000001`, RLS por `user_id`, `on delete cascade` desde `documents`) guarda la conversación completa por documento, a diferencia de `ai_sessions` que solo registra un log agregado por llamada. `lib/ai/prompts/document-chat.ts` construye el prompt con el texto del documento (truncado a 12k chars, igual que APA/writing) + hasta 10 turnos previos de contexto + la pregunta nueva; usa `generateText` (texto libre) en vez de `generateStructured`, ya que una respuesta de chat no tiene forma de JSON fija. Restringido a DOCX por ahora, igual que los otros dos tools (PDF/XLSX/CSV no tienen extracción de texto implementada todavía — ver sección 1.1, `pdfjs-dist`/`xlsx` siguen sin instalarse). UI (`DocumentChat`) con burbujas de mensaje diferenciadas por rol, auto-scroll al último mensaje, y selector de documento por página (`/tools/chat-documento` sin query param lista documentos; `?documentId=...` abre la conversación de ese documento). Verificado: (1) capa de base de datos end-to-end con un usuario y documento reales (insert/select con RLS respetada, cascada de borrado), usuario de prueba eliminado después; (2) `buildDocumentChatPrompt` verificado con un caso de ejemplo; (3) la llamada real a `generateText` no se pudo verificar en este momento porque la cuota gratuita diaria de Gemini (20/día) ya estaba agotada por las pruebas de la Fase 8 — el error se manejó correctamente (mismo comportamiento ya verificado antes), pero la respuesta exitosa de un turno de chat real queda pendiente de confirmar cuando la cuota se restablezca o se configure `GROQ_API_KEY`. Lint, typecheck y build sin errores.
- **Fase 10 en progreso** (7 herramientas de estudiante; se prioriza una por una con el usuario en vez de construirlas todas de golpe):
  - **Resumir documento** (`/tools/resumir`) completada: resumen breve, detallado, ideas clave (hasta 10) y conclusiones, generados en una sola llamada estructurada a IA. **Decisión de arquitectura**: a diferencia de APA/escritura, este tool NO sigue la arquitectura híbrida reglas+IA de la sección 1.2 — resumir no tiene un "formato correcto" objetivamente verificable por reglas deterministas, es 100% generativo, y se documenta así explícitamente en `lib/documents/analyze/summarize.ts` para que quede claro que la ausencia de reglas es intencional, no un olvido. UI con botón de copiar por sección. Verificado end-to-end con un documento real: Gemini falló por cuota agotada (esperado, mismo límite gratuito de siempre) y el fallback a Groq respondió correctamente con un resumen de alta calidad — primera confirmación en producción real de que el fallback funciona con el modelo `openai/gpt-oss-120b` corregido.
  - **Corrector de redacción** (`/tools/corrector-redaccion`) completada: arquitectura híbrida completa (a diferencia de "Resumir documento"). Reglas mecánicas deterministas (`lib/rules/redaccion.ts`): espacios dobles, palabras repetidas consecutivas, puntuación pegada al texto siguiente, oraciones largas (>40 palabras). La IA (`lib/ai/prompts/redaccion.ts`) cubre ortografía y gramática real —no hay corrector ortográfico de español instalado en el proyecto, así que esa parte depende necesariamente del modelo de lenguaje—, citando el fragmento original y la corrección específica en cada hallazgo. Verificado end-to-end con un documento con errores deliberados (concordancia de género/número, artículos redundantes "del el", tildes faltantes, "avia" por "había"): Gemini falló por cuota (esperado) y Groq corrigió con precisión los 9 errores plantados, más las 3 reglas mecánicas correctas.
  - **Generador de referencias APA** (`/tools/referencias-apa`) completada. **Decisión de arquitectura importante**: esta es la primera herramienta que NO usa IA en absoluto — el formato de una referencia APA 7 es una regla fija y determinista, no algo que deba "generar" un modelo de lenguaje (riesgo real de alucinar formato o datos). Todo el formateo vive en `lib/citations/apa-reference.ts` (funciones puras, sin `"use server"`, invocables directo desde el cliente para feedback instantáneo mientras se escribe). Soporta 4 tipos de fuente (página web, libro, artículo de revista, tesis), con reglas APA 7 completas de lista de autores (`&` antes del último, elipsis "..." si hay más de 20 autores). Búsqueda automática por DOI vía Crossref (`api.crossref.org`, pública y gratuita, sin API key) y autocompletado best-effort desde URL (`<title>` + `og:site_name`) en `lib/citations/lookup.ts`. Copiado al portapapeles con `ClipboardItem` (`text/plain` + `text/html`) para preservar cursivas al pegar en Word. **Bug real encontrado y corregido durante la verificación**: los nombres de autor ya terminan en "." (la inicial, ej. "Ray, O.") y los títulos reales de Crossref a veces también terminan en punto — las plantillas iniciales agregaban un punto de cierre sin verificar, produciendo "Ray, O.." y "Título.." Se agregó `withPeriod()`/lógica equivalente para evitar puntuación duplicada, verificado con un DOI real (`10.1037/0003-066X.59.1.29`) antes y después del fix.
  - **Analizador de bibliografía** (`/tools/bibliografia`) completada. Igual que el generador de referencias, **sin IA**: comparar si una cita tiene o no una entrada correspondiente en la lista de referencias es un hecho verificable por texto (dentro de los límites del reconocimiento de patrones), no un juicio que se beneficie de un modelo de lenguaje. `lib/rules/bibliografia.ts` extrae citas parentéticas ("(Autor, Año)", incluyendo múltiples separadas por ";") y narrativas ("Autor (Año)") del cuerpo, extrae entradas de la sección "Referencias" (una por párrafo), y las cruza por año + coincidencia de al menos un token de apellido. Reporta citas sin referencia (error) y referencias no citadas (warning). Sin llamada a IA: no consume el límite diario ni se registra en `ai_sessions`. Verificado con un documento de prueba con 3 desajustes deliberados (una cita sin referencia en formato parentético, una en formato narrativo, y una referencia nunca citada) — los 3 detectados correctamente, sin falsos positivos en las 2 citas válidas.
  - **Preparador de exposiciones** (`/tools/exposiciones`) completada: 100% generativo (igual que "Resumir documento", sin capa de reglas). Una sola llamada estructurada devuelve estructura de secciones con puntos clave, un guion de exposición oral completo en primera persona (no un resumen del documento, sino lo que la persona diría en voz alta), y hasta 8 posibles preguntas de evaluador/audiencia con respuesta sugerida. Verificado end-to-end con un documento real: Gemini falló por cuota (patrón esperado), Groq generó una estructura de 7 secciones coherente, un guion natural bien hilado, y 7 preguntas relevantes con respuestas razonables.
  - **Generador de presentaciones** (`/tools/presentaciones`) completada: 100% generativo, mismo patrón que "Resumir documento" y "Preparador de exposiciones". Genera entre 6 y 15 diapositivas (título, agenda, contenido por tema, cierre), cada una con título, hasta 6 bullets como frases cortas (no oraciones completas — se le pide explícitamente al prompt, ya que un bullet de diapositiva no se parece a un resumen en prosa) y notas de orador. **No genera un archivo `.pptx` descargable** — eso requeriría agregar una librería de generación de presentaciones no evaluada (fuera del alcance de esta fase); en su lugar, la UI ofrece "Copiar todo" para trasladar el contenido al editor de presentaciones del usuario. Verificado end-to-end con un documento real sobre reciclaje de plásticos: Groq (tras el fallo esperado de cuota de Gemini) generó 9 diapositivas bien estructuradas con bullets efectivamente cortos, no prosa.
  - **Ayudante de programación** (`/tools/programacion`) completada — última herramienta de la fase. **Diferente arquitectura al resto de la Fase 10**: no procesa un documento subido, el código se pega directo en un textarea (no tiene sentido pedir subir un .docx para pegar código). Cuatro acciones (Explicar / Corregir / Mejorar / Documentar) comparten un solo schema de salida (`summary`, `explanation`, `findings`, `resultCode`) pero el prompt le indica al modelo qué llenar según la acción — ej. "Explicar" no modifica el código, "Documentar" no corrige bugs de lógica aunque puede señalarlos como advertencia. Como no hay `document_id` real, no se inserta en `document_analyses` (violaría su FK not-null); solo se registra en `ai_sessions` para el límite diario e historial. Verificado con código real con un bug deliberado (`i <= array.length`, fuera de rango): las tres acciones probadas (explicar/corregir/documentar) identificaron el bug correctamente, "corregir" lo arregló junto con un segundo problema real (división por cero) que ni siquiera se había plantado a propósito, y "documentar" agregó JSDoc sin alterar la lógica, marcando el bug como advertencia aparte.
- **Fase 10 completa**: las 8 herramientas de estudiante del catálogo (`STUDENT_TOOLS`) están implementadas y disponibles.
- **Corrección de infraestructura de IA**: el modelo `llama-3.3-70b-versatile` (configurado como default de Groq desde la Fase 5) fue retirado por Groq — confirmado vía `GET /openai/v1/models` con la API key real del usuario, ya no aparece en el listado. Reemplazado por `openai/gpt-oss-120b`, verificado en vivo en modo texto plano y JSON estructurado. `GROQ_API_KEY` quedó configurada por primera vez en este punto del proyecto, habilitando el fallback real (antes solo Gemini estaba activo).

## 1. Arquitectura propuesta

### 1.1 Stack

| Capa | Elección | Motivo |
|---|---|---|
| Framework | Next.js (App Router) + TypeScript | SSR/RSC, API routes integradas, deploy directo a Vercel free tier |
| Estilos | Tailwind CSS + shadcn/ui + lucide-react | Componentes accesibles, sin licencia, alto control visual |
| Auth | Supabase Auth (Google OAuth) | Free tier suficiente, integra con Postgres + RLS |
| Base de datos | Supabase PostgreSQL | Free tier, RLS nativo |
| Storage | Supabase Storage | Free tier (1GB), integrado con Auth/RLS |
| IA primaria | Gemini API (free tier) | Buen free tier, multimodal, soporta JSON estructurado |
| IA secundaria/fallback | Groq (free tier, modelos OSS) | Rápido, gratuito, útil como fallback si Gemini falla o se agota cuota |
| DOCX | `docx` (generación) + `mammoth` (lectura) | Librerías maduras, sin costo |
| PDF | `pdf-lib` (manipulación) + `pdfjs-dist` (extracción texto) | Cobertura lectura+escritura |
| Excel/CSV | `xlsx` (SheetJS) | Estándar de facto — instalado desde `cdn.sheetjs.com`, no desde npm (ver Fase 11, incidente de seguridad evitado) |
| Deploy | Vercel (free tier) | Integración nativa Next.js |

### 1.2 Regla de arquitectura híbrida (reglas + IA)

Cada "analizador" (ej. APA) se divide en:
- **Motor de reglas** (`/lib/rules/*`): validaciones deterministas (márgenes, fuente, interlineado, estructura, patrones de referencia vía regex/heurísticas). No consume tokens de IA.
- **Motor de IA** (`/lib/ai/*`): evaluación de redacción, coherencia, cohesión, recomendaciones contextuales. Solo recibe el texto/metadatos estrictamente necesarios.
- Los resultados de ambos motores se combinan en un **`AnalysisResult`** tipado común antes de mostrarse en UI.

### 1.3 Capa de abstracción de IA (`/lib/ai/`)

```
lib/ai/
  provider.ts       // interfaz AIProvider (generate, generateStructured, embed?)
  gemini.ts         // implementación GeminiProvider
  groq.ts           // implementación GroqProvider (fallback)
  service.ts         // AIService: selecciona provider, maneja fallback y logging
  prompts/
    apa.ts
    writing.ts
    academic.ts
    professional.ts
    document-chat.ts
    ...
```

`AIService` es el único punto de entrada usado por los módulos de negocio (server actions / route handlers). Nunca se importa `GeminiProvider`/`GroqProvider` directamente fuera de esta capa. Cambiar de proveedor principal = cambiar una config, no código de negocio.

### 1.4 Procesamiento de documentos (`/lib/documents/`)

Separado en fases explícitas por módulo:
```
lib/documents/
  extract/     // DOCX/PDF/XLSX -> texto/estructura plana
  analyze/     // reglas programadas + orquestación IA
  generate/    // generación de DOCX/PDF de salida
```

### 1.5 Estructura de carpetas (Next.js App Router)

```
/app
  /(marketing)/page.tsx            -> landing "/"
  /login/page.tsx
  /onboarding/page.tsx
  /(app)/dashboard/page.tsx
  /(app)/documents/page.tsx
  /(app)/tools/page.tsx
  /(app)/tools/[slug]/page.tsx
  /(app)/history/page.tsx
  /(app)/settings/page.tsx
  /(app)/profile/page.tsx
  /api/...                         -> route handlers (uploads, ai, webhooks si aplica)
/components
  /ui                              -> shadcn primitives
  /shared                          -> componentes compartidos (cards, empty-states, etc.)
  /dashboard, /documents, /tools   -> componentes por dominio
/lib
  /ai
  /documents
  /rules
  /supabase                        -> clients server/browser, middleware helpers
  /config                          -> límites centralizados (MAX_AI_REQUESTS_PER_DAY, etc.)
  /types
/supabase
  /migrations
/docs
  ARCHITECTURE.md, DATABASE.md, AUTH.md, AI.md, FILE_PROCESSING.md, SECURITY.md, COSTS.md, DEVELOPMENT.md
```

### 1.6 Configuración centralizada de límites

`/lib/config/limits.ts` exporta constantes tipadas (`MAX_AI_REQUESTS_PER_DAY`, `MAX_DOCUMENTS_PER_DAY`, `MAX_FILE_SIZE_MB`), leídas desde env vars con defaults seguros. Ningún módulo hardcodea estos valores.

### 1.7 Variables de entorno (definidas, no inventadas)

```
NEXT_PUBLIC_SUPABASE_URL
NEXT_PUBLIC_SUPABASE_ANON_KEY
SUPABASE_SERVICE_ROLE_KEY        # solo servidor, nunca en cliente
GEMINI_API_KEY                   # solo servidor
GROQ_API_KEY                     # solo servidor
MAX_AI_REQUESTS_PER_DAY
MAX_DOCUMENTS_PER_DAY
MAX_FILE_SIZE_MB
```
Se documentan en `.env.example` cuando se cree en Fase 1/2.

## 2. Fases de desarrollo

- [x] **Fase 1** — Base del proyecto + UI + arquitectura (Next.js, TS, Tailwind, shadcn, estructura de carpetas, landing estática, .env.example, docs base) — completada
- [x] **Fase 2** — Supabase + Auth por email/contraseña (cliente Supabase, proxy.ts, signup/login/logout, confirmación de correo, protección de rutas) — completada y verificada end-to-end por el usuario
- [x] **Fase 3** — Profiles + onboarding + dashboard (onboarding, layout con sidebar, dashboard real, perfil, configuración) — completada y verificada por el usuario
- [x] **Fase 4** — Sistema de documentos + Storage (tabla `documents`, bucket privado con RLS por carpeta de usuario, subida con validación de tipo/tamaño/firma de bytes, listado, descarga con URL firmada, eliminación) — completada
- [x] **Fase 5** — Capa de IA (`AIProvider`, `GeminiProvider`, `GroqProvider`, `AIService`) — completada y verificada contra la API real; prompts base se crean en la Fase 6 junto con su primer consumidor
- [x] **Fase 6** — Corrector APA (reglas programadas + IA, diagnóstico UI) — completada y verificada por el usuario con documentos reales
- [x] **Fase 7** — Generación DOCX corregido — completada y verificada de extremo a extremo con la API real de Gemini
- [x] **Fase 8** — Analizador de escritura / indicadores de IA — completada; ver decisión ética en la bitácora (no es un detector de IA, son indicadores de estilo)
- [x] **Fase 9** — Chat con documentos — completada; llamada real a la IA pendiente de confirmar por cuota diaria agotada (ver bitácora)
- [x] **Fase 10** — Resto de herramientas de estudiante: Resumir documento, Corrector de redacción, Generador de referencias APA, Analizador de bibliografía, Preparador de exposiciones, Generador de presentaciones, Ayudante de programación — las 8 herramientas de estudiante del catálogo completas
- [x] **Fase 11** — Herramientas profesionales: Asistente de correos, Analizador de CV, Generador de CV, Resumen de reuniones, Generador de documentos, Analizador de documentos, Analizador de Excel/CSV, Generador de informes — las 8 herramientas profesionales del catálogo completas
  - **Asistente de correos** (`/tools/correos`) completada: mismo patrón sin documento que el Ayudante de programación (texto pegado directo, no hay archivo que subir). Cuatro acciones (Redactar / Mejorar / Resumir / Cambiar tono) comparten un schema (`subject`, `body`, `notes`); para "Redactar" el texto de entrada es un brief/instrucciones, no un correo ya escrito — distinción explícita en el prompt para que la IA no intente "mejorar" un texto que en realidad son instrucciones. Verificado con las 3 acciones más representativas: "Redactar" generó un correo formal completo con asunto a partir de un brief informal; "Mejorar" corrigió gramática/ortografía y agregó saludo/despedida sin inventar contenido; "Cambiar tono" convirtió un correo formal a uno cercano preservando la intención.
  - **Analizador de CV** (`/tools/analizador-cv`) completada: arquitectura híbrida completa (mismo patrón que APA/redacción). Reglas deterministas (`lib/rules/cv.ts`): datos de contacto (correo/teléfono), presencia de secciones comunes (Experiencia/Educación/Habilidades), extensión, densidad de logros cuantificados (números/porcentajes por cada 100 palabras) y consistencia de formato de fechas. La IA (`lib/ai/prompts/cv.ts`) evalúa solo lo que requiere criterio: claridad, impacto real de los logros descritos (vs. responsabilidades genéricas), consistencia de tono/tiempo verbal, y vacíos temporales entre empleos. Verificado con un CV de prueba deliberadamente débil (sin correo, sin sección Educación, muy breve, fechas en formatos mixtos): las 4 reglas detectaron exactamente lo esperado, y la IA además señaló espontáneamente una superposición real de fechas entre dos empleos que no se había plantado como caso de prueba explícito.
  - **Generador de CV** (`/tools/generador-cv`) completada: sin documento subido, el usuario ingresa datos personales estructurados (nombre, correo, teléfono, ubicación, puesto objetivo) + notas libres de experiencia/educación/habilidades (texto sin formato, como las escribiría el usuario de forma natural), y la IA las estructura y redacta en un CV profesional. Regla explícita en el prompt: no inventar empresas, cargos, fechas ni logros que no estén implícitos en las notas — si falta un dato (ej. nombre de una empresa), debe indicarlo como no especificado en vez de fabricarlo. Verificado con notas realistas y desordenadas: estructuró correctamente las dos experiencias, preservó la métrica real mencionada por el usuario (ahorro de 5 horas/semana) sin inventar cifras adicionales, generó el perfil profesional automáticamente (se dejó vacío a propósito en la prueba), y — comportamiento correcto que confirma que la regla anti-invención funciona — escribió "nombre no especificado" para la tienda de ropa en vez de inventar un nombre de empresa.
  - **Resumen de reuniones** (`/tools/reuniones`) completada: sin documento, transcripción/notas pegadas directo (mismo patrón que Correos/Programación/Generador de CV). Extrae resumen, decisiones concretas (no discusiones sin resolver), tareas pendientes con responsable y fecha, y otros puntos relevantes. Misma regla anti-invención que el generador de CV: si el responsable o la fecha de una tarea no se mencionan explícitamente, usar "No especificado" en vez de inventarlos. Verificado con notas de reunión reales con un caso deliberado de tarea sin responsable claro ("alguien tiene que contactar al proveedor... no quedó claro quién"): correctamente marcó "No especificado" sin inventar un nombre. **Imperfección observada y documentada honestamente**: para la tarea de Carlos, el texto sí mencionaba "para el próximo viernes" como fecha, pero el modelo la reportó como "No especificado" en lugar de extraerla — un error de omisión (más seguro que inventar), no de invención, pero indica que el prompt podría reforzarse para extraer fechas relativas mencionadas explícitamente.
  - **Generador de documentos** (`/tools/documentos`) completada: sin documento de entrada, pero **es la primera herramienta sin documento que produce un archivo DOCX real descargable**, a diferencia de Correos/Programación/Generador de CV/Reuniones que solo muestran texto. `lib/documents/generate/documento-docx.ts` construye el DOCX desde cero con la librería `docx` (ya instalada desde la Fase 1, sin dependencias nuevas) a partir del JSON estructurado que devuelve la IA (título + secciones con encabezado opcional). El archivo se sube a Storage y se inserta en `documents`, así que también aparece en "Mis documentos" — valida tanto el límite diario de IA como el de documentos creados. Soporta informe, memorando, carta y propuesta, cada uno con guía de formato distinta en el prompt. Verificado con un memorando real: el contenido usó correctamente el placeholder "[fecha]" en vez de inventar una fecha no proporcionada, y el DOCX generado se abrió y extrajo sin corrupción (verificado con mammoth).
  - **Analizador de documentos** (`/tools/analizador-documentos`) completada. **Diferente a los demás tools basados en documento**: no diagnostica problemas (no tiene sentido un "score" para una extracción de datos), así que no reutiliza `FindingSection`/`RuleFinding` — tiene su propia UI de 4 categorías (Fechas, Montos, Personas/partes, Obligaciones). Fechas y montos se extraen por patrones deterministas (`lib/rules/analizador-documentos.ts`); personas/partes y obligaciones requieren comprensión semántica y las extrae la IA. **Regla ética explícita en el prompt**: solo extracción factual neutral, sin evaluar si una cláusula es riesgosa o desfavorable ni dar ningún tipo de consejo legal — eso lo debe hacer un profesional humano; disclaimer visible en la UI antes de mostrar resultados. Verificado con un contrato de arrendamiento de prueba: extrajo correctamente ambas fechas (formatos distintos: "01/04/2024" y "15 de marzo de 2024"), ambos montos ("$8,500" y "10%"), ambas partes con su rol correcto (Arrendador/Arrendataria), y las dos obligaciones correctamente atribuidas a la parte correspondiente, sin ningún comentario de riesgo o juicio legal.
  - **Dependencia nueva con incidente de seguridad evitado**: al instalar `xlsx` (SheetJS) desde npm para esta herramienta, `npm install` reportó una vulnerabilidad de severidad alta sin fix disponible en el registro de npm (prototype pollution + ReDoS explotables con un archivo malicioso — riesgo real para esta app, que acepta archivos subidos por usuarios). Investigado: SheetJS dejó de publicar las versiones parchadas en npm y las distribuye desde su propio dominio. Se desinstaló el paquete de npm y se instaló la versión oficial parchada (`0.20.3`) directamente desde `cdn.sheetjs.com` (`npm install https://cdn.sheetjs.com/xlsx-latest/xlsx-latest.tgz`, reflejado en `package.json`). `npm audit` confirma 0 vulnerabilidades tras el cambio.
  - **Analizador de Excel/CSV** (`/tools/excel-csv`) completada. Estadísticas por columna (conteo, suma, promedio, mín, máx para numéricas; cardinalidad para texto) son 100% deterministas (`lib/documents/extract/spreadsheet.ts`, parseo con SheetJS soportando tanto XLSX binario como CSV); la IA (`lib/ai/prompts/excel.ts`) solo redacta una narrativa de tendencias e inconsistencias **sobre las estadísticas ya calculadas**, con instrucción explícita de no recalcularlas ni inventar cifras nuevas. Verificado con un CSV de prueba con un outlier deliberado (una venta de 15,000 contra el resto entre 1,000-1,300): las estadísticas deterministas fueron exactas (suma 19,600, promedio 3,920, mín/máx correctos), y la IA identificó el outlier repetidamente en "tendencias" y "hallazgos notables" citando siempre las cifras reales, sin inventar ninguna. También verificado el parseo de XLSX binario real (no solo CSV) con datos numéricos decimales.
  - **Generador de informes** (`/tools/informes`) completada — última herramienta de la fase. Reutiliza el mismo análisis determinista + IA del Analizador de Excel/CSV (`analyzeSpreadsheet`) pero lo empaqueta como un DOCX real descargable con una **tabla de estadísticas de verdad** (usando `Table`/`TableRow`/`TableCell` de la librería `docx`, no solo texto) además de las secciones de resumen, tendencias y hallazgos — a diferencia del Analizador de Excel/CSV que solo muestra los resultados en pantalla. Como "Generador de documentos", el DOCX se sube a Storage y se registra en `documents`, validando ambos límites diarios (IA y documentos). El PDF mencionado en el catálogo original queda fuera de alcance por ahora (`pdf-lib`/`pdfjs-dist` no están instalados, mismo criterio aplicado con `xlsx`: no se agregan dependencias nuevas sin evaluarlas explícitamente). Verificado con datos de ventas de prueba: el DOCX generado se abrió y extrajo sin corrupción (verificado con mammoth), la tabla de estadísticas incluyó las cifras reales correctas, y el análisis de tendencias citó exactamente esas cifras sin inventar ninguna.
- **Fase 11 completa**: las 8 herramientas profesionales del catálogo (`PROFESSIONAL_TOOLS`) están implementadas y disponibles. Sumadas a las 10 de estudiante (`STUDENT_TOOLS`) de la Fase 10, **las 18 herramientas del catálogo original están completas — todo `components/tools/catalog.ts` está marcado `available: true`**.
- **Fase 12 completada**: historial + estadísticas de uso reales, sin tablas nuevas — `ai_sessions` (ya poblada desde la Fase 6 por cada herramienta) y `documents` eran suficiente base. `lib/usage/queries.ts` centraliza `getUsageSummary` (solicitudes de IA y documentos subidos hoy vs. límites), `getRecentAiSessions` y `getToolUsageCounts`, todas memoizadas por request con `cache()` igual que `lib/dal.ts`/`lib/documents/queries.ts`. `/history` (antes `ComingSoon`) muestra barras de uso diario, herramientas más usadas y actividad reciente real, usando `getToolBySlug()` (nuevo helper en `catalog.ts`) para resolver ícono/nombre desde el `tool` guardado en `ai_sessions`. El dashboard también se conectó a datos reales: las tarjetas "Actividad reciente" y "Herramientas más utilizadas", placeholders desde la Fase 3, ahora muestran las últimas sesiones y el conteo real por herramienta; se quitó la tarjeta "el dashboard sigue en construcción" al dejar de haber placeholders. **Bug real encontrado y corregido**: el Generador de documentos registraba sus llamadas en `ai_sessions` con `tool: "generador-documentos"`, que no coincide con el slug `"documentos"` del catálogo — sin este fix, esa herramienta habría aparecido como entrada "huérfana" (sin ícono ni nombre) en el historial y en "más utilizadas". **Simplificación aplicada de paso**: la función `startOfTodayIso()` (límite de "por día" en UTC) estaba duplicada de forma idéntica en 17 archivos de `lib/tools/*/actions.ts` y `lib/documents/*actions.ts`; se centralizó en `lib/config/limits.ts` junto a `LIMITS`, que ya era la fuente de verdad de los valores de límite, y se refactorizaron los 17 archivos para importarla. Verificado con build real: las páginas `/dashboard` y `/history` compilan como rutas dinámicas (`ƒ`) sin errores, y ambas redirigen correctamente a `/login` sin sesión (307, confirmado con curl). Verificación visual en navegador con datos reales confirmada por el usuario.
- [x] **Fase 12** — Historial + límites + estadísticas de uso
- **Fase 13 completada**: auditoría de seguridad manual de todo el código (sin remoto/rama para diff de PR — hubo que revisar el repo completo en vez de un diff). Encontró y corrigió 3 hallazgos reales: SSRF en la búsqueda de referencias por URL (`lib/citations/lookup.ts`, el servidor hacía `fetch()` a cualquier URL del usuario sin validar protocolo, resolución DNS ni bloqueo de rangos privados/metadata de nube), `getDownloadUrl` sin verificación de dueño (`lib/documents/actions.ts`, única acción del proyecto sin ese check — no explotable hoy por RLS de Storage pero sin defensa en profundidad), y un esquema `javascript:` sin filtrar en el `href` de referencias APA generadas (`lib/citations/apa-reference.ts`). El resto de la auditoría (RLS, límites de IA, subida de archivos, secretos, manejo de errores, generación de DOCX) no tuvo hallazgos — detalle completo en `docs/SECURITY.md`. **Se instaló Vitest** (primer framework de test del proyecto; requirió subir `@types/node` de `^20` a `^24` para que coincidiera con el Node real en uso — la versión anterior nunca se había ajustado desde la Fase 1) con 98 tests sobre la lógica 100% pura y determinista: las 6 reglas de `lib/rules/*` (APA, redacción, CV, escritura, bibliografía, analizador de documentos), el formateo de referencias APA (incluyendo el fix de `javascript:` recién hecho), la validación de archivos por firma de bytes, y el guard SSRF recién escrito (verificado también con un script standalone contra 5 casos reales antes de integrarlo a la suite). Las herramientas con IA real siguen sin tests automatizados — se mantiene el patrón de verificación manual end-to-end contra Gemini/Groq usado en todo el proyecto, documentado explícitamente en `docs/DEVELOPMENT.md` como decisión consciente (mockear la IA solo probaría el mock). `npm test` sumado al flujo de cierre de fase.
- [x] **Fase 13** — Seguridad + pruebas
- **Correcciones post-Fase 13 (reportadas por el usuario en uso real)**:
  - **Descarga de DOCX rota en Informes/Documentos/CV**: tres causas distintas encontradas en serie, cada una diagnosticada con evidencia real (no adivinada) antes de corregirse. (1) `createSignedUrl` sin la opción `download` — el navegador intentaba mostrar el `.docx` inline en vez de descargarlo, y como no sabe renderizarlo, la pestaña quedaba en blanco; corregido pasando `{ download: nombreDeArchivo }`. (2) Invocar un Server Action directo desde un `onClick` (sin `<form>` ni `startTransition`) resultó no despacharse de forma fiable en esta versión de Next.js — confirmado agregando un log incondicional al inicio de la función y viendo que nunca aparecía en el servidor pese a que el usuario reintentó varias veces con reinicios completos. Se reemplazaron los 2 flujos de descarga (`getDownloadUrl`, `generateCorrectedDocument`) por rutas API convencionales (`GET /api/documents/download-url`, `POST /api/tools/apa/generate-corrected`) invocadas con `fetch()` normal, que no dependen de IDs de acción cifrados ni del mecanismo de despacho de Server Actions. (3) Con la ruta API ya funcionando, el navegador seguía sin descargar — la causa real era `window.open()` bloqueado por el navegador (confirmado viendo en el log del servidor que la petición SÍ llegaba y devolvía 200, pero la pestaña abierta se quedaba en `about:blank`); se eliminó `window.open()` de los 4 lugares que lo usaban y se reemplazó por un `<a>` temporal con `click()` programático (`triggerFileDownload()` en `lib/documents/client.ts`), que no está sujeto a bloqueo de ventanas emergentes.
  - **Generador de CV ahora descarga un DOCX real**: antes solo mostraba el resultado en pantalla con un botón "Copiar". Se agregó `lib/documents/generate/cv-docx.ts` (mismo patrón que documento-docx.ts/informe-docx.ts) con estructura visual de CV profesional real: nombre grande + línea de contacto con separador, secciones con título en mayúsculas y línea divisoria, fechas alineadas al margen derecho junto al cargo/título (tab stop), empresa/institución en cursiva, logros con viñetas. `runGeneradorCv()` ahora también valida `MAX_DOCUMENTS_PER_DAY` al registrar el archivo en `documents`, igual que Documentos/Informes. Verificado con un script standalone: XML válido, sin corrupción, bordes/tabs/color de acento presentes en el DOCX generado.
  - Durante las pruebas de estos fixes se agotó el límite diario de IA y de documentos varias veces (esperado, ligado a la cuota gratuita de Gemini) — se subieron temporalmente `MAX_AI_REQUESTS_PER_DAY`/`MAX_DOCUMENTS_PER_DAY` en `.env.local` para poder seguir probando en la misma sesión; sin costo real, ya que ambos proveedores de IA son free-tier sin facturación configurada (ver `docs/COSTS.md`).
- **Rediseño visual retro estilo Windows 98 (pedido explícito del usuario, primera fase)**: tema global en `app/globals.css` con paleta clásica (escritorio verde azulado, paneles grises, barra de título azul marino), `--radius: 0px` (aplana todos los `rounded-*` de golpe porque ya se derivaban de esa variable), y clases `.win98-panel`/`.win98-well`/`.win98-btn`/`.win98-titlebar` con bisel 3D vía `box-shadow` inset apilado. Las primitivas compartidas (Button, Card, Input, Badge) se actualizaron una sola vez y esto se propaga automáticamente a las 18 herramientas. Nuevo `components/retro/title-bar.tsx` (`TitleBar`/`RetroWindow`) usado para convertir el shell de `(app)` y las páginas públicas (login, signup, forgot/reset-password, onboarding) en "ventanas" con barra de título real. Landing page con header convertido a barra de título. Alcance de esta primera pasada: sistema de diseño + shell + páginas públicas + campos de formulario de las 18 herramientas (vía script, se les agregó `win98-well` a todos los `<select>`/`<textarea>` nativos).
  - **Segunda pasada de pulido** (pedida explícitamente por el usuario: "terminemos todo antes de subir" — completar lo ya construido antes de tocar Fase 15): barrido sistemático con `grep` de `shadow-md`/`hover:shadow`/`rounded-full`/`hover:-translate-y` en todo `app/` y `components/` para encontrar conflictos donde una utilidad de Tailwind (capa de utilidades, se aplica después) pisaba el bisel de `.win98-panel` (capa de componentes) sobre la misma propiedad `box-shadow` — mismo bug ya visto en la landing page, esta vez en dashboard, chat con documentos y el box de "Generar DOCX corregido" de APA. Barras de progreso (`finding-section.tsx`, `/history`) rediseñadas de píldora redondeada a barra cuadrada con fondo hundido (`win98-well`), ya que `rounded-full` es un valor fijo de Tailwind que no se deriva de `--radius` y por eso no se aplanó solo. Chat con documentos: contenedor de mensajes y burbujas convertidos a panel hundido/elevado; el `<input>` de pregunta (no usaba el componente `Input` compartido) recibió el mismo tratamiento `win98-well` que ya se le había dado a los 17 `<select>`/`<textarea>` en la primera pasada — se había quedado fuera porque el script original solo apuntaba a archivos `*-form.tsx`. Tabla de "Mis documentos" y su estado vacío convertidos a panel hundido tipo visor de archivos. Toggle de tema y selector de rol en Configuración (botones nativos, no el componente `Button`) recibidos el bisel `win98-btn` con estado seleccionado azul marino, igual que la navegación lateral. Bloque de código resultante del Ayudante de programación convertido a visor hundido. Se dejaron deliberadamente sin bisel los ítems de lista dentro de tarjetas ya biseladas (hallazgos de reglas, obligaciones, preguntas de exposición, tareas de reuniones) — ya se ven correctos solo con el aplanado de `--radius: 0` y el color de `--border`, y darles bisel propio se vería como "ventana dentro de ventana". Se corrigió además el copy desactualizado de `/documents` que aún decía "disponibles en próximas fases" siendo que las 18 herramientas están completas desde la Fase 11 (mismo tipo de hallazgo que ya se había corregido en `/tools` durante la Fase 14).
- **Fase 14 completada**: sin acceso a navegador para medir con Lighthouse, se priorizaron optimizaciones concretas y verificables por código/build.
  - **Streaming de navegación**: `app/(app)/loading.tsx` — al no existir ningún `loading.tsx` en todo el proyecto, cada navegación dentro de la app (dashboard, historial, documentos, las 18 herramientas, settings, perfil) dejaba la pantalla en blanco mientras el Server Component resolvía sus datos. Un solo archivo en el segmento `(app)` envuelve automáticamente todas sus rutas hijas en un límite de Suspense (convención de Next.js), sin tocar cada página individualmente.
  - **Resiliencia**: `app/(app)/error.tsx` — antes un error no controlado en cualquier página autenticada mostraba la pantalla de error genérica de Next.js sin salida; ahora hay un boundary con botón "Reintentar" para todo el segmento.
  - **Metadata por página**: cada una de las ~31 rutas (18 herramientas + dashboard/documentos/historial/etc. + páginas públicas) compartía el mismo `<title>` "NEXA AI" desde la Fase 1 — imposible distinguir pestañas con varias páginas abiertas. Se agregó `title.template: "%s · NEXA AI"` en el layout raíz y un `export const metadata` por página (script para las 18 herramientas + 13 páginas restantes, usando los nombres ya existentes en `catalog.ts`).
  - **Código muerto eliminado**: el campo `available` de `ToolCatalogItem` (y sus dos únicos consumidores, en `/tools` y el dashboard) quedó vestigial desde que la Fase 11 completó las 18 herramientas — todas las entradas del catálogo ya tenían `available: true` sin excepción, así que la rama "Próximamente" nunca se ejecutaba. Se quitó el campo y ambas ramas muertas, y se corrigió el copy de `/tools` que todavía decía "se irán activando fase a fase" siendo que el catálogo está completo. También se quitaron 5 SVGs de `public/` (file.svg, globe.svg, next.svg, vercel.svg, window.svg) — sobras del scaffolding de `create-next-app` de la Fase 1, confirmado sin referencias en todo el código antes de borrarlos.
  - **Auditoría sin hallazgos que arreglar** (documentado para no repetir la revisión después): bundle de cliente ~1MB en chunks (razonable para 39 rutas con React 19 + Next 16), `docx`/`xlsx`/`mammoth`/`jszip` confirmados sin fugas al bundle de cliente (todos detrás de `server-only`, cero imports en archivos `.tsx`); cero Client Components innecesarios (los 38 archivos con `"use client"` son formularios, nav con `usePathname`, o primitivas de `@base-ui/react` que lo requieren intrínsecamente); cero `<img>` sin `next/image` (no hay imágenes en la app, solo íconos SVG de `lucide-react`); `npm audit` en 0 vulnerabilidades; índices de base de datos ya cubren los patrones de consulta reales (`ai_sessions_user_id_created_at_idx` cubre las 3 queries de `lib/usage/queries.ts`; `document_analyses` no tiene índice por `user_id` pero tampoco tiene ningún `SELECT` en todo el código — solo se inserta, nunca se lee, así que no hay problema de rendimiento que resolver ahí todavía).
- [x] **Fase 14** — Optimización
- **Fase 15 completada**: repositorio privado creado en GitHub (`eduA006/nexa-ai`) vía `gh` CLI y push del código; proyecto de Vercel creado e importado por el usuario directamente desde su dashboard (las herramientas MCP de Vercel/Supabase disponibles en este entorno resultaron estar conectadas a cuentas distintas a las del usuario — `get_auth_user` devolvía 404 y `list_projects` mostraba un proyecto de Supabase ajeno, "SISTEMA INSTITUCIONAL" — así que la conexión GitHub↔Vercel, la carga de variables de entorno y el registro del dominio en Supabase se hicieron manualmente desde los dashboards reales del usuario, no por herramienta). Variables de entorno cargadas directamente en el dashboard de Vercel por el usuario (nunca pegadas en el chat, seguido consistente con la regla del proyecto). Se corrigió de paso un modelo de Groq desactualizado en `.env.example` (`llama-3.3-70b-versatile`, retirado desde la Fase 10/11) que se habría copiado tal cual de no revisarlo.
  - **Bug real encontrado y corregido, específico de producción multi-instancia**: tras el primer deploy, el onboarding (elegir Estudiante/Profesional) entraba en loop — seleccionar un rol llevaba de vuelta a la misma pantalla, y con cuenta nueva el clic ni siquiera disparaba una petición de red. Diagnóstico remoto paso a paso (sin acceso directo a la cuenta de Vercel/Supabase del usuario, todo vía que el usuario revisara dashboards/DevTools y reportara lo que veía): se descartó primero un `Site URL` mal configurado en Supabase (tenía `.../auth/callback` en vez del dominio base — bug real independiente, corregido igual), pero el onboarding seguía roto incluso con cuenta nueva y con el `role` confirmado como correctamente guardado en la tabla `profiles`. Causa raíz real: `setRole()` se invoca vía `.bind(null, "/dashboard")` para pasarle un argumento extra al Server Action (mismo patrón en `sendChatMessage.bind(null, documentId)` del chat con documentos y en el selector de rol de Configuración) — Next.js encripta los valores capturados así ("closure encryption"), y su documentación es explícita: en despliegues multi-instancia hace falta fijar `NEXT_SERVER_ACTIONS_ENCRYPTION_KEY` a un valor estable, o cada instancia genera su propia clave al azar y las peticiones manejadas por instancias distintas no logran descifrarse entre sí. Vercel es multi-instancia por diseño; el entorno de desarrollo local no (un solo proceso), por eso nunca se manifestó hasta el deploy real pese a que Fase 3 ya había verificado el onboarding funcionando en local. Se generó una clave nueva (`openssl rand -base64 32`) y se agregó como variable de entorno en Vercel — resuelto, confirmado por el usuario. Variable documentada en `.env.example` (sin el valor real) para que el próximo despliegue no vuelva a pisar esto a ciegas.
- [x] **Fase 15** — Deploy (Vercel)
- **Flashcards (primera herramienta del backlog priorizada)**: mismo patrón 100% generativo que "Resumir documento" (sin capa de reglas — no hay un conjunto "correcto" de tarjetas verificable determinísticamente). A partir de un DOCX, la IA genera entre 8 y 25 tarjetas de pregunta/respuesta cubriendo los conceptos del documento, con la misma regla anti-invención que el resto de herramientas generativas (basarse solo en lo que dice el texto). UI interactiva de estudio: una tarjeta a la vez, clic para revelar la respuesta, navegación anterior/siguiente y reinicio — en vez de listar todas las tarjetas de una (que no serviría para practicar recuerdo activo). Se agregó `Layers` como ícono nuevo del catálogo.
  - **Reorganización del catálogo por categorías** (pedida explícitamente por el usuario junto con Flashcards: "no quiero que salgan las herramientas todas amontonadas"): `ToolCatalogItem` ahora tiene un campo `category`, y `/tools` agrupa las herramientas del perfil del usuario en secciones con encabezado en vez de una sola grilla plana. Estudiante quedó en 6 categorías (Formato y citas, Escritura, Documentos, Exposiciones, Estudio, Programación) y Profesional en 4 (CV y carrera, Documentos e informes, Datos, Comunicación). El catálogo pasó de 18 a 19 herramientas totales (11 estudiante + 8 profesional).
  - **Segunda vuelta — pedida explícitamente ("inventa algún método innovador para clasificarlas sin que se vea básico")**: el agrupamiento con encabezados se reemplazó por una vista de "carpetas" estilo Explorador de Windows (`components/tools/tool-folders.tsx`): cada categoría es un ícono de carpeta (abierta/cerrada según estado) que al hacer clic despliega una `RetroWindow` propia con la grilla de esa categoría — coherente con el tema retro en vez de un acordeón genérico. Se extendió `TitleBar`/`RetroWindow` (`components/retro/title-bar.tsx`) para aceptar un `onClose` opcional que vuelve funcional el botón "×" (antes 100% decorativo en las 8 páginas que ya lo usaban desde el rediseño retro), reutilizado aquí para cerrar la carpeta abierta. También se rediseñó visualmente el visor de Flashcards ("mejora más las flashcards que sean visualmente mejores"): animación de volteo 3D real (CSS `perspective`/`backface-visibility`/`rotateY`, nuevas clases en `globals.css`) en vez de un cambio de texto instantáneo, efecto de pila con 1-2 tarjetas "fantasma" biseladas detrás de la actual (sugiriendo cuántas faltan), y una barra de progreso `win98-well` mostrando avance en el mazo.
  - **Tercera vuelta — rediseño de Flashcards rechazado explícitamente** ("no me gusta cómo se ve, quiero algo distinto"): se presentaron 3 alternativas con previews ASCII vía pregunta directa y el usuario eligió "Ficha de estudio física" sobre "Modo terminal/DOS" y "Tablero tipo memorama". Se reemplazó el volteo 3D por un componente `IndexCard` que imita una ficha de estudio en papel: línea roja de margen a la izquierda (`bg-red-400/60`), fondo color papel (`#fdf8e8`), pregunta arriba y botón "Ver respuesta" que revela la respuesta debajo con una línea punteada divisoria, en vez de voltear la tarjeta. Se quitaron las clases CSS del volteo 3D (`.flip-card-*`) de `globals.css` por quedar muertas.
- **Simulador de exámenes** (backlog priorizado explícitamente: "Simulador de examen, se sube documento y ejemplos de posibles exámenes, la IA evalua tus resultados"): arquitectura híbrida (como APA/redacción/CV) — la IA genera entre 5 y 15 preguntas mixtas (opción múltiple y abiertas, ~mitad y mitad) a partir de un DOCX y, opcionalmente, texto de exámenes anteriores pegado por el usuario (para imitar formato/dificultad, nunca como fuente de contenido). La calificación es híbrida: opción múltiple se corrige de forma determinista (comparar índice elegido vs. correcto, sin IA — es un hecho verificable), preguntas abiertas se califican con una sola llamada a IA que evalúa las N respuestas abiertas del intento en lote (acepta paráfrasis, no exige texto exacto). Diseño de seguridad explícito: el set de preguntas con respuestas correctas se guarda solo en `document_analyses` (`analysis_type: "examen"`) y nunca se envía al cliente hasta después de calificar — el formulario de calificación reenvía únicamente `examId` + respuestas del usuario, y el servidor vuelve a leer las preguntas confiables desde la base de datos (nunca confía en datos de preguntas que pudiera mandar el cliente). Bug de estado detectado y corregido antes de probar (por lección aprendida del bug de serialización RSC de `/tools`): la UI se separó en `ExamenForm` (generación) + un hijo `ExamSession` montado con `key={examId}`, para que cada examen nuevo arranque con un `useActionState` de calificación limpio en vez de arrastrar el resultado calificado del intento anterior. Verificado con un script standalone contra la API real (Gemini caído con 503 en el momento de la prueba, fallback automático a Groq confirmado funcionando en producción real, no solo en teoría) antes de dar la herramienta por terminada. Se agregó `ClipboardCheck` como ícono nuevo del catálogo, categoría "Estudio" (junto a Flashcards).
- **Tema "Moderno" como alternativa al retro** (pedido explícito: "mejora el frontend, pero no lo cambies, déjalo como un tema... un botón para alternar entre modo retro y modo moderno"), usando el skill `redesign-existing-projects` instalado vía `npx skills add`: se reutilizó la infraestructura de `next-themes` que ya estaba instalada pero vestigial (un selector claro/oscuro/sistema en Ajustes que no hacía nada real desde el rediseño retro, porque `.dark` tenía la misma paleta que `:root`). Se repurpuso para dos temas con nombre (`retro`/`modern`, `enableSystem={false}`, default `retro`) en vez de claro/oscuro. El tema moderno es una "piel" nueva sobre los mismos componentes `.win98-panel`/`.win98-well`/`.win98-btn`/`.win98-titlebar` — se sobreescriben sus variables de color (paleta editorial cálida, radios reales de 0.75rem, un solo acento desaturado derivado del azul marino de marca) y sus box-shadow (biseles pixelados → sombras suaves con transición) bajo el selector `.modern`, sin tocar ni un componente ni la lógica de negocio, tal como pidió el usuario. El bloque `.dark` (paleta duplicada, ya muerto) se reemplazó por `.modern`. Botón compacto `components/theme-mode-toggle.tsx` (ícono único que alterna, sin menú) colocado en el sidebar de escritorio, la barra superior móvil y el header de la landing pública; el selector completo de Ajustes (`components/settings/theme-toggle.tsx`) se simplificó de 3 opciones (claro/oscuro/sistema) a 2 (Retro/Moderno). Verificado con build + SSR (sin acceso a navegador real en esta sesión — sin extensión de Chrome conectada): el HTML servido confirma que el componente y el script anti-parpadeo de `next-themes` están presentes; falta que el usuario confirme visualmente en su propio navegador.
- **Suscripciones — solo la lógica de planes, sin cobro real todavía** (decisión explícita del usuario vía preguntas dirigidas: alcance "solo la lógica de planes" primero, gating por "acceso a herramientas específicas" en vez de límites diarios distintos, y el subconjunto de herramientas Pro elegido por Claude — "las que veas más útiles"). Se agregó la columna `profiles.plan` (`'free'`/`'pro'`, default `'free'`) vía `supabase/migrations/20260922000001_add_profiles_plan.sql`, aplicada a la base de datos real (`sjlkaorjmzwbcdlayspb`/NEXAAI) con `supabase db push` — confirmado con `supabase migration list` que quedó sincronizada local↔remoto. **Decisión de seguridad importante**: la policy `profiles_update_own` ya existente permite al usuario actualizar cualquier columna de su propia fila (la usa el formulario de rol) — sin protección adicional, cualquier usuario autenticado podría auto-otorgarse Pro llamando a Supabase directo desde el cliente. Se agregó un trigger `protect_profile_plan` que revierte cualquier cambio a `plan` que no venga del rol `service_role` (dashboard de Supabase o, más adelante, el webhook de pagos) — el usuario prueba planes hoy cambiando `plan` directo en el Table Editor de Supabase (bypassa RLS), no hay botón de auto-upgrade en la app. Herramientas elegidas para Pro (`lib/config/plans.ts`, `PRO_TOOL_SLUGS`): Generador de CV, Generador de documentos, Generador de informes, Analizador de documentos, Generador de presentaciones, Simulador de exámenes — criterio: generan un entregable real o son las de mayor complejidad de IA; el resto del catálogo (incluye Flashcards, Chat con documentos, Resumir, Corrector APA, Preparador de exposiciones, etc.) sigue gratis. Gating aplicado en **dos capas** (no solo UI): `components/plans/pro-gate.tsx` (Server Component que envuelve el formulario en cada page.tsx de las 6 herramientas y muestra `UpgradePrompt` en vez del formulario si no hay acceso) + el mismo chequeo repetido dentro de cada Server Action correspondiente (`lib/tools/<tool>/actions.ts`, mismo patrón que ya usan para el chequeo de sesión) — la UI oculta el formulario, pero sin el chequeo en la Server Action un usuario Free igual podría invocarla directo. Badge "Pro" visible en `/tools`, el panel de recomendadas del dashboard, y una tarjeta "Plan" nueva en Ajustes que lista las herramientas Pro y el estado actual (Free/Pro) — copy honesto sobre que el cobro real todavía no está activo, sin botón de compra falso.
- **Analizador de riesgos y contratos** (backlog priorizado explícitamente, con dos decisiones tomadas vía preguntas dirigidas antes de construir): profesional, plan Pro (agregada a `PRO_TOOL_SLUGS`, misma línea que Analizador de documentos), categoría "Documentos e informes", ícono `ShieldAlert`. **Tensión ética explícita resuelta**: el Analizador de documentos ya tenía la regla "NO evalúa riesgo de cláusulas, solo extrae hechos" — esta herramienta hace lo opuesto a propósito, así que se decidió con el usuario el enfoque exacto: "patrones + disclaimer" en vez de un score de riesgo numérico. El prompt (`lib/ai/prompts/riesgos-contratos.ts`) marca cláusulas con `deviation: "leve"|"notable"|"significativa"` (qué tan atípica es la redacción frente a prácticas contractuales comunes) y prohíbe explícitamente palabras como "legal/ilegal" o "válido/inválido" — nunca es un veredicto legal, solo comparación de patrones de redacción, con disclaimer obligatorio en la UI (mismo patrón que el Analizador de escritura). 100% generativo (no hay regla determinista posible para "qué tan típica es una cláusula", a diferencia de fechas/montos en el Analizador de documentos). Verificado con un script standalone contra la API real con un contrato de prueba con cláusulas deliberadamente atípicas (responsabilidad ilimitada, cesión sin consentimiento) — la IA las detectó correctamente y las explicó sin emitir juicios legales en ningún momento (Gemini caído con 503 otra vez, fallback a Groq confirmado). El catálogo pasó de 20 a 21 herramientas (12 estudiante + 9 profesional).
- **Pulido visual de la lógica de planes** (pedido explícito: "quiero que el apartado de plan se vea mejor y también lo que aparece cuando quieres usar una herramienta de pago"): la tarjeta "Plan" de Ajustes pasó de una lista plana de badges a un comparativo Free/Pro de dos columnas (`win98-well`, con `ring-2 ring-primary` marcando cuál es el plan activo, ícono `Zap`/`Crown`, checklist de herramientas de cada lado). `UpgradePrompt` (lo que ve un usuario Free al abrir una herramienta Pro) pasó de un ícono de candado + texto plano a un círculo de acento con `Crown`, y agrega cross-sell de las demás herramientas Pro como badges — mismo objetivo (dejar claro qué es Pro y adónde ir), mejor jerarquía visual. Ambos reutilizan tokens de tema (`win98-well`, `ring-primary`, `bg-card`) así que se reskin automáticamente en modo Moderno sin cambios adicionales.
- **Cobro real con Yape (pago manual, sin API)** — pedido explícito: "quiero que sea por yape". **Decisión técnica importante confirmada con el usuario antes de construir**: Yape no tiene API pública de autoservicio (requiere RUC + convenio comercial con BCP para "Yape para Negocios"), así que se implementó el único flujo realista sin esa integración: el usuario yapea manualmente al QR/número del dueño de la app, sube una captura del comprobante, y un admin la aprueba/rechaza desde un panel dentro de la propia app — sin mover dinero ni verlo Claude en ningún momento.
  - **Pro pasó de "para siempre" a expirar** (S/ 5 semanales, sin renovación automática): se agregó `profiles.pro_expires_at` (`supabase/migrations/20260922000002_add_pro_expiry_and_payment_requests.sql`, aplicada a producción con `supabase db push`, tipos regenerados con `supabase gen types typescript --linked` en vez de a mano). El trigger `protect_profile_plan` (de la migración anterior) se reemplazó por una versión que protege **ambas** columnas (`plan` y `pro_expires_at`) contra el cliente autenticado normal — sin esto, un usuario con Pro vencido podría auto-extenderse la fecha de expiración indefinidamente aunque no pudiera tocar `plan` directamente. `hasProAccess()`/`canAccessTool()` (`lib/config/plans.ts`) cambiaron de firma: ya no reciben solo el string de `plan`, reciben el perfil completo (`{ plan, pro_expires_at }`) para poder comparar la fecha — se actualizaron los 7 Server Actions de herramientas Pro y los 4 puntos de UI que los llamaban.
  - **Tabla `payment_requests`** (RLS: el usuario ve/inserta solo las suyas, sin policy de update — el cambio de estado pasa exclusivamente por `service_role`) + bucket privado `payment-proofs` para las capturas, mismo patrón RLS por carpeta que el bucket `documents`.
  - **Primer uso real de un cliente `service_role`** en el proyecto (`lib/supabase/admin.ts`, ya estaba documentada la variable `SUPABASE_SERVICE_ROLE_KEY` en `.env.example` desde Fase 2 pero nunca se había usado) — necesario porque aprobar un pago implica escribir `plan`/`pro_expires_at` (bloqueado para el cliente normal por el trigger) y leer comprobantes de TODOS los usuarios (bloqueado por RLS del bucket).
  - **Identificación de admin sin rol en la base de datos**: `ADMIN_EMAILS` (env var, coma-separado, `lib/config/admin.ts`) comparado server-side contra el email de la sesión — se decidió así en vez de una columna `is_admin` en `profiles` para no tener que proteger una segunda columna con el mismo problema de auto-escritura que ya resolvió el trigger de `plan`.
  - **Panel `/admin/pagos`**: lista solicitudes pendientes con nombre/correo del usuario y una URL firmada (5 min) del comprobante vía `next/image` (`unoptimized`, porque optimizar una URL que expira en minutos no tiene sentido) — requirió agregar `images.remotePatterns` para `*.supabase.co/storage/v1/object/sign/**` en `next.config.ts`, primera imagen raster real de la app (hasta ahora solo había íconos SVG). Aprobar activa Pro por 7 días exactos desde el momento de aprobación (no desde que se subió el comprobante). Enlace "Pagos (admin)" visible solo en el sidebar de escritorio para el email en `ADMIN_EMAILS`.
  - **QR real del usuario** (`public/yape-qr.jpg`, exportado desde su app de Yape) en vez de intentar generar uno — Claude no generó ni fabricó el QR: no hay documentación pública del formato de QR de Yape, así que inventar uno habría mostrado un código no funcional a usuarios pagando de verdad.
  - Verificado con un script standalone contra la base de datos real (no contra IA esta vez): confirmó que el bucket `payment-proofs` acepta subidas y genera URLs firmadas, que el trigger permite escribir `plan`/`pro_expires_at` desde `service_role`, y ejecutó el flujo completo de aprobación (insertar solicitud → aprobar → verificar `plan: "pro"`) sobre un perfil real, revirtiendo todo al finalizar.
  - `ADMIN_EMAILS=eduardoodev@gmail.com` confirmado por el usuario, cargado en `.env.local` y en Vercel (Settings → Environments, guiado paso a paso porque el dashboard renombró esa sección). Carpeta `QR YAPE/` de la raíz eliminada a pedido del usuario (ya no se usaba, se había copiado a `public/yape-qr.jpg`).
- **Pulido del flujo de Yape** (pedido explícito: "agregale animación y que salga en pantalla, una vez que se apruebe, que salte confeti y un mensaje de cuenta pro activada"): el QR pasó de una imagen estática a un botón animado (`animate-in fade-in-0 zoom-in-95`, hover con hint "Toca para ampliar") que abre un lightbox centrado (`components/plans/qr-lightbox.tsx`, sobre el primitivo `@base-ui/react/dialog` — el mismo que ya usaba `Sheet`, reutilizado con posición centrada en vez de panel lateral) con el QR a tamaño grande.
  - **Festejo en vivo, no solo al recargar**: se instaló `canvas-confetti` y se habilitó **Supabase Realtime** por primera vez en el proyecto (`supabase/migrations/20260922000003_enable_payment_requests_realtime.sql`, `alter publication supabase_realtime add table payment_requests`) — Realtime respeta la policy `payment_requests_select_own` ya existente automáticamente, así que cada usuario solo recibe el cambio de su propia fila aunque la escritura la haga el admin vía `service_role`. `components/plans/pro-celebration.tsx` se suscribe a `postgres_changes` (evento UPDATE) filtrado a `user_id=eq.<id>` desde `/settings`; al ver `status: "approved"` dispara confeti + un banner "¡Cuenta Pro activada!" y `router.refresh()` para que el resto de la página (comparativo Free/Pro, fecha de expiración) se actualice sin recargar manualmente. También cubre el caso de que la aprobación haya ocurrido mientras el usuario no estaba en la página (revisa `latestRequest` al montar) — y usa `localStorage` por id de solicitud para no repetir el festejo en cada visita a Ajustes mientras el Pro sigue activo.
  - Verificado con un script standalone contra la base de datos real: insertó una solicitud, la marcó `approved` con el cliente `service_role`, y confirmó que un listener de Realtime suscrito como el del usuario real recibió el evento `UPDATE` end-to-end antes de revertir todo.

Cada fase se cierra solo tras: lint + typecheck + build sin errores críticos, documentación de la fase, y actualización de este archivo.

## 3. Decisiones tomadas sin consultar (documentadas)

- **Login con email + contraseña en vez de Google OAuth.** El plan original pedía Google. Se cambió porque crear el OAuth Client ID en Google Cloud Console exige verificar una tarjeta, y el usuario prefirió evitarlo. Email + contraseña es nativo de Supabase Auth (confirmación por correo incluida), sin consolas externas ni verificación de pago. Decisión tomada junto con el usuario el 2026-09-19. Google (o GitHub, que no requiere tarjeta) puede añadirse después sin rediseñar la capa de auth. Ver `docs/AUTH.md`.
- Se usa `mammoth` para lectura de DOCX (extracción a texto/HTML) por ser la opción gratuita más madura; `docx` para generación.
- Se usa `pdfjs-dist` para extracción de texto de PDF y `pdf-lib` para manipulación/generación, ya que cubren necesidades distintas y ambas son gratuitas.
- Row Level Security se implementará desde la primera migración que cree tablas con `user_id`, no se pospone.
- No se crea remoto de GitHub automáticamente; se inicializa git local. Se preguntará antes de crear/push a un repositorio remoto.

## 4. Próximo paso inmediato

Las 15 fases del plan original están completas. Próximo paso: priorizar ideas del backlog (sección 5) si el usuario quiere seguir construyendo, o mantenimiento/monitoreo del deploy en producción.

## 5. Backlog de ideas futuras (sin priorizar, sin comprometer)

Ideas aportadas por el usuario el 2026-09-19, transcritas tal cual para no perderlas. **Ninguna está planificada todavía** — no aparecen en el dashboard ni en `/tools` hasta que se prioricen explícitamente y se les asigne una fase. Varias requieren evaluar costo/API antes de aceptarlas (ver `docs/COSTS.md`).

Estudiante:
- ~~Flashcards~~ — **construida el 2026-09-22**, ver sección 2 (Fase 16 informal, primera herramienta del backlog priorizada)
- Organizador de horario con técnica Pomodoro
- ~~Practicar exámenes con retroalimentación~~ — **construida el 2026-09-22** como "Simulador de exámenes", ver sección 2
- Tutor con IA que analiza el material y evalúa con preguntas estratégicas
- Función predictiva de conflictos de fechas de entrega
- Crear plan de estudio
- Silenciar notificaciones
- Escáner de estilo de redacción
- "Cápsula de conocimiento": extraer información de un documento y generar audio (texto a voz, posiblemente vía Gemini)
- Asistente para trabajos en grupo (reparto equitativo de tareas)
- Función de debate
- Auditor de coherencia del documento
- Simulador de defensa/sustentación con preguntas difíciles
- Herramienta de síntesis bibliográfica para armar el marco teórico más rápido
- Rastreador de hábitos de estudio

Profesional:
- Agente de resumen de reuniones con extracción de tareas pendientes (posible solape con la herramienta 8.8 ya planificada)
- Conectar datos de Excel y generar informe, permitiendo al usuario elegir qué gráficos generar
- ~~Analizador de riesgos y contratos~~ — **construida el 2026-09-22**, ver sección 2
- Asistente de correo con contexto, sincronizado con calendario; usar API de Gemini para mejorar la redacción
- Optimizador de presentaciones: el usuario ingresa texto, el sistema sugiere cómo quedaría y genera la diapositiva (el usuario mencionó una "API de ztai" sin más detalle — no se investiga ni se asume nada de esta API hasta que el usuario la confirme)
- Analizador de tendencias de mercado
- Generador de actas de juntas directivas

Antes de mover cualquiera de estas a una fase real: confirmar con el usuario prioridad, alcance exacto, y si requiere una API o costo adicional no cubierto en `docs/COSTS.md`.
